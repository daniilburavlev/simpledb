[package]
name = "buffer"
edition = "2024"
version.workspace = true

[dependencies]
common = { path = "../common" }
file = { path = "../file" }
log = { path = "../log" }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
